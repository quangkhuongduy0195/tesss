using System;
using System.Collections.Generic;
using System.Diagnostics;
using ViewModels.BaseScreen;
using Xamarin.Forms;

namespace Views.View
{
    public partial class contentImage : RelativeLayout
    {
        public contentImage()
        {
            InitializeComponent();
            //var widthScreen = Application.Current.MainPage.Width;
            //var heightScreen = Application.Current.MainPage.Height;
            //img1.WidthRequest = (widthScreen / 2) - 7;
            //img1.HeightRequest =img1.WidthRequest / 2;
            //img2.WidthRequest = (widthScreen / 2) - 7;
            //img2.HeightRequest = img2.WidthRequest / 2 ;
            //img1.Margin = new Thickness(5,0,2,0);
            //img2.Margin = new Thickness(2, 0, 5, 0);
            //BaseScreen.
            //lbl.FontSize = Device.GetNamedSize(NamedSize.Large, typeof(Label));
        }
    }
}
